# .env — Environment Variables (do not commit to git)
# Copy this to .env and fill in your keys

# Groq API Key (for cloud generation)
GROQ_API_KEY=_YOUR_API_KEY

# Optional: Other keys if needed later
# OPENAI_API_KEY=sk-...
